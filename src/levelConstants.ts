import { GameConstants } from "./gameConstants";

export class LevelConstants {
  static readonly MIN_LEVEL_W = 6;
  static readonly MIN_LEVEL_H = 6;

  static readonly MAX_LEVEL_W = 11;
  static readonly MAX_LEVEL_H = 11;

  static readonly SCREEN_W = 17; // screen size in tiles
  static readonly SCREEN_H = 17; // screen size in tiles

  static readonly ENVIRONMENTS = 4;
}
